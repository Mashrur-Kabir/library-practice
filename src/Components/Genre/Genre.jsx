import PropTypes from 'prop-types';

const Genre = ({genre}) => {

    console.log(genre);

    // tbc...........

    return (
        <div>
            
        </div>
    );
};

Genre.propTypes = {
    genre: PropTypes.string.isRequired,
}

export default Genre;